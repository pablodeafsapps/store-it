# Implement (/speckit.implement)

/speckit.implement

When implementing, follow the post-AGP 9.0 structure: all shared and Android code lives in `:shared`. Use paths under `shared/src/commonMain/`, `shared/src/androidMain/`, and `shared/src/commonTest/` as per plan.md and tasks.md.
